{
  "name": "nft-project",
  "version": "1.0.0",
  "description": "Project for creating and minting NFTs",
  "scripts": {
    "generate-metadata": "node scripts/generateMetadata.js",
    "deploy": "npx hardhat run scripts/deploy.js --network goerli"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@openzeppelin/contracts": "^4.9.0",
    "hardhat": "^2.16.0"
  },
  "dependencies": {
    "alchemy-sdk": "^3.5.0",
    "dotenv": "^16.4.7"
  }
}
